# OpenLane metrics summary

First full RTL→GDS run succeeded on GitHub Actions:

- Run: https://github.com/beginnerof/asic-flow-lab/actions/runs/34697562747
- Job: `rtl-to-gds` (LibreLane 3.0.14 + sky130A, CLOCK_PERIOD=40 ns)
- Wall time: ~1h06m

Download the **metrics-summary** artifact from that run (or re-run the `openlane` workflow) and paste the table below.

| metric | value |
|--------|-------|
| _(pending artifact download)_ | |

Full GDS and step logs are in the large `openlane-asic-flow-lab` artifact of the same run.
